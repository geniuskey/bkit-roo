{
  "name": "bkit-roo",
  "private": true,
  "description": "Backend Kit for Roo - Platform-independent core modules extracted from Roo Code",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean",
    "format": "prettier --write \"packages/*/src/**/*.ts\"",
    "format:check": "prettier --check \"packages/*/src/**/*.ts\"",
    "check-vscode": "! grep -r 'import.*vscode' packages/*/src/ && echo 'No vscode imports found'"
  }
}
